// Insert and remove element in array

var ara = [1, 2, 3, 4, 5, 6, 7, 8]

// // If we use push method then it insert value at last
// ara.push(9,10)
// // If we use push method then it insert value at first
// ara.unshift(11)

// ara.splice(3, 0, 4)

// // If we use push method then it delete value at last
// ara.pop()
// // If we use push method then it delete value at first
// ara.shift()

ara.splice(3,1)
console.log(ara)